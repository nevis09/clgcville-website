name = "clgcville-website"
type = "javascript"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Environment variables for all environments
[env.production]
vars = { }

# Secrets are configured in Cloudflare Dashboard
# GITHUB_TOKEN should be set as a secret in Cloudflare Pages project settings
